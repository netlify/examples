[build]
  publish = "public"
  ignore = "git diff $CACHED_COMMIT_REF $COMMIT_REF -- examples/serverless/functions-hello-world"

[dev]
  publish = "public"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
